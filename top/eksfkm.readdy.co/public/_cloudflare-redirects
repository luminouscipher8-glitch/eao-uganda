# Cloudflare Pages Redirects Configuration
# API proxy to backend (Render) - MUST come first!
/api/*  https://eao-uganda.onrender.com/api/:splat  200

# SPA routing - MUST come after API proxy
/*      /index.html                                 200
